# Learning Hub Repository Instructions

## このリポジトリの責務

このリポジトリは4つの学習用システムを横断管理する学習ハブである。アプリケーションコードを実装しない。全体計画、計画変更管理、共通知識マップ、学習進捗、リポジトリ間の対応関係だけを管理する。

## 正本

- 上位計画: `bootstrap/master-plan.md`
- 計画変更規則: `bootstrap/plan-governance.md`
- リポジトリ対応: `registry/repositories.md`
- 横断状態: `status/portfolio-status.md`
- 横断クイズ索引: `quiz-progress-index.md`
- 承認済み横断決定: `governance/decision-log.md`

各アプリ固有の実装計画、状態、仕様、検証証跡、クイズ回答原文は各アプリリポジトリを正本とする。ハブへ詳細を複製しない。

## 作業開始時の確認

変更前に、少なくとも次を確認する。

1. `bootstrap/master-plan.md`の`PLAN_VERSION`
2. `bootstrap/plan-governance.md`
3. `governance/decision-log.md`
4. `registry/repositories.md`と`status/portfolio-status.md`
5. 対象作業が上位計画の変更か、通常の文書保守か

今回の許可範囲と禁止範囲を明示し、依頼と承認済み計画に差異があれば変更を開始せず報告する。

## 上位計画の変更

`PROPOSED_CHANGE`は、`CAREER-SYSTEMS-V1`の業務範囲、技術構成、実施順など、上位計画そのものを変更する場合にだけ使用する。

上位計画の変更が必要な場合は、現行計画との差分、理由、影響、代替案を提示し、ユーザーの明示承認を待つ。承認前に`bootstrap/master-plan.md`を変更したり、変更後の計画に基づく作業を開始したりしない。計画全体を独断で再作成・置換しない。

正本構造の整備、誤記修正、リンク更新、事実に基づく状態更新は通常の文書作業として扱う。ただし、通常作業を装って上位計画の意味を変更してはならない。

## 状態と根拠

- 計画上の予定と実際の状態を分離する。
- `実装済み`は対象コードの存在、`検証済み`は検証結果の証跡、`完了済み`は承認済み完成条件の充足を根拠とする。
- 検証を再実行していない場合は、過去の検証記録を確認したことと、今回再検証したことを区別する。
- remote確認で`git fetch`していない場合は、ローカルのremote追跡参照との一致に限定して記述する。
- 不明な状態を推測で補わない。

## 安全とスコープ

- アプリケーションコード、依存関係、アプリ固有仕様をこのハブへ追加しない。
- 各アプリリポジトリへの変更は、ユーザーから個別に許可された場合だけ行う。
- push、Pull Request、デプロイ、GitHub設定変更、CI自動trigger、外部サービス接続は明示指示なしに行わない。
- ユーザーの変更と無関係なworktree変更を保持する。
- commitは明示指示がある場合だけ行う。
